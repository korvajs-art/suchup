name = "suchup"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# Create with: npx wrangler d1 create suchup
# Then replace database_id below with the real remote ID.
[[d1_databases]]
binding = "DB"
database_name = "suchup"
database_id = "9f002233-afe4-4d33-acac-d7cb3c5a1484"
preview_database_id = "9f002233-afe4-4d33-acac-d7cb3c5a1484"

# Sessions use random HttpOnly cookies (hashed in D1). No shared secret in repo.
# Set production secrets only in Cloudflare Pages → Settings → Environment variables.
